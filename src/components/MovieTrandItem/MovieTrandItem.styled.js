import styled from 'styled-components';

export const MoviesTrandsItem = styled.li`
  /* display: block;
  width: 260px;
  height: 380px;
  text-align: center;
  margin: 0;
  padding: 0; */
  /* display: grid;
  place-items: center; */

  /* grid-template-columns: repeat(160px 160px 160px 160px);
  grid-column-gap: 15px;
  grid-row-gap: 1em; */
  border-radius: 10px;
`;

export const TrendMovieTirle = styled.h2`
  text-decoration: none;
  font-weight: 700;
  font-size: 18px;
  line-height: 1.3;
`;
